/**
 * The software's version and the phase of its release, as the About
 * dialog and the shell bar's tag name them. Bumped in the commit that
 * tags a release, so a tester can say which build they are on.
 */

export const VERSION = '1.0.0-beta.1';
export const PHASE = 'Private beta';
